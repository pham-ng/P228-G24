node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal
data.db.*

# Automated backup scheduler output and manual snapshots — same rule as data.db:
# real guest names, phones, emails and folio charges. Never commit.
backups/

.env
.env.*
!.env.example

*.log
.DS_Store

# Local tooling, not project source
.claude/
ollama.exe
ollama.zip
ollama-windows-amd64.zip
OllamaSetup.exe
ollama_bin/
lib/ollama/

# Benchmark DB snapshots/checkpoints — binary, large, demo data at rest.
# The JSON/MD reports next to them (the actual methodology record) stay tracked.
bench/baselines/**/*.db
bench/baselines/**/*.db.*
bench/baselines/*.db

# Scratch/probe files
.pplx-probe.txt
_live.ts
_smoke.ts

# Coverage reports — regenerate with `npm run test:coverage`
coverage/

# Speech model weights (886 MB). Fetched on first use into models/hf by
# server/stt.ts; seed an air-gapped box by copying the directory, not by
# committing it.
models/

# Token cho Prometheus doc /api/metrics — khong bao gio commit
ops/scrape-token

# Giong doc Piper: 375 MB nhi phan + trong so, tai bang models/piper/README.md
models/piper/piper/
models/piper/voices/

# Môi trường ảo Python cho phiên âm tiếng Nhật (tái tạo bằng docs/SETUP-VOICE.md)
.venv-tts-ja/
